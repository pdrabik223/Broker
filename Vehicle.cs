namespace broker;

public class Vehicle
{
    // asks broker for new book path 
    // has internal private queue with list of factories to visit
    // and for each factory, witch books to remove 
    // than visits Consumers  and removes books from internal storage 
}